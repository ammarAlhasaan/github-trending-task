import React, { useState } from 'react';
import { PageTitle } from '../../components';

export const HomePage = () => (
  <div>
    <PageTitle title="Home" description="It's just a Home page." />

  </div>
);
